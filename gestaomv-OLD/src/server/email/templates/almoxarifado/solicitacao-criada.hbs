<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>Nova Solicitação de Material</title>
    <style>
        body { font-family: Arial, sans-serif; line-height: 1.6; color: #333; }
        .container { max-width: 650px; margin: 0 auto; padding: 20px; }
        .header { background: linear-gradient(135deg, #3b82f6, #1e40af); padding: 30px; border-radius: 8px; margin-bottom: 20px; text-align: center; color: white; }
        .content { background: white; padding: 30px; border: 1px solid #dee2e6; border-radius: 8px; }
        .solicitacao-info { background: #f8fafc; padding: 20px; border-radius: 6px; margin: 20px 0; border-left: 4px solid #3b82f6; }
        .material-item { background: #f9fafb; padding: 12px; border-radius: 4px; margin: 8px 0; border: 1px solid #e5e7eb; }
        .footer { margin-top: 20px; padding-top: 20px; border-top: 1px solid #dee2e6; font-size: 12px; color: #6c757d; text-align: center; }
        .btn { display: inline-block; background: #3b82f6; color: white; padding: 12px 24px; text-decoration: none; border-radius: 6px; font-weight: bold; margin: 10px 5px; }
        .btn:hover { background: #2563eb; }
        .btn-approve { background: #10b981; }
        .btn-approve:hover { background: #059669; }
        .btn-reject { background: #ef4444; }
        .btn-reject:hover { background: #dc2626; }
        .badge { background: #3b82f6; color: white; padding: 4px 8px; border-radius: 12px; font-size: 12px; font-weight: bold; }
        .total-value { background: #f0f9ff; padding: 15px; border-radius: 6px; text-align: center; margin: 15px 0; border: 2px solid #3b82f6; }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <div style="font-size: 48px; margin-bottom: 10px;">📋</div>
            <h1 style="margin: 0; font-size: 28px;">Nova Solicitação de Material</h1>
            <p style="margin: 10px 0 0 0; font-size: 16px; opacity: 0.9;">Aguardando sua aprovação</p>
        </div>
        
        <div class="content">
            <h2>Olá, {{aprovadorNome}}!</h2>
            
            <p>Uma nova solicitação de material foi criada e precisa da sua aprovação.</p>
            
            <div class="solicitacao-info">
                <h3 style="margin-top: 0; color: #3b82f6;">📋 Informações da Solicitação</h3>
                <p><strong>Número:</strong> <span class="badge">#{{formatNumber solicitacao.id}}</span></p>
                <p><strong>Solicitante:</strong> {{solicitacao.solicitante.name}} ({{solicitacao.solicitante.email}})</p>
                <p><strong>Unidade de Destino:</strong> {{solicitacao.unidade.nome}}</p>
                <p><strong>Data da Solicitação:</strong> {{formatDate solicitacao.dataOperacao}}</p>
                {{#if solicitacao.observacoes}}
                <p><strong>Observações:</strong> {{solicitacao.observacoes}}</p>
                {{/if}}
            </div>
            
            <h3>📦 Materiais Solicitados</h3>
            {{#each materiais}}
            <div class="material-item">
                <div style="display: flex; justify-content: space-between; align-items: center;">
                    <div>
                        <strong>{{material.nome}}</strong>
                        <br>
                        <small style="color: #6b7280;">{{material.tipoMaterial.nome}} • {{material.unidadeMedida.nome}}</small>
                    </div>
                    <div style="text-align: right;">
                        <strong>{{qtdSolicitada}} {{material.unidadeMedida.simbolo}}</strong>
                        <br>
                        <small style="color: #059669;">{{formatCurrency material.valorUnitario}} / unid.</small>
                    </div>
                </div>
            </div>
            {{/each}}
            
            <div class="total-value">
                <h3 style="margin: 0; color: #3b82f6;">💰 Valor Total Estimado</h3>
                <p style="font-size: 24px; font-weight: bold; margin: 5px 0; color: #1e40af;">{{formatCurrency valorTotal}}</p>
            </div>
            
            <h3>⚡ Ações Rápidas</h3>
            <p>Clique nos botões abaixo para aprovar ou rejeitar esta solicitação diretamente:</p>
            
            <div style="text-align: center; margin: 30px 0;">
                <a href="{{systemUrl}}/admin/almoxarifado/solicitacoes/{{solicitacao.id}}?action=approve" class="btn btn-approve">
                    ✅ Aprovar Solicitação
                </a>
                <a href="{{systemUrl}}/admin/almoxarifado/solicitacoes/{{solicitacao.id}}?action=reject" class="btn btn-reject">
                    ❌ Rejeitar Solicitação
                </a>
            </div>
            
            <div style="text-align: center; margin: 20px 0;">
                <a href="{{systemUrl}}/admin/almoxarifado/solicitacoes/{{solicitacao.id}}" class="btn">
                    👁️ Ver Detalhes Completos
                </a>
            </div>
            
            <div style="margin-top: 20px; padding: 15px; background: #fef3c7; border-radius: 6px; border-left: 4px solid #f59e0b;">
                <p style="margin: 0;"><strong>⏰ Lembrete:</strong> Solicitações aguardando aprovação por muito tempo podem afetar a produtividade da equipe.</p>
            </div>
        </div>
        
        <div class="footer">
            <p>Esta é uma notificação automática do Sistema de Gestão de Almoxarifado.</p>
            <p>Para gerenciar suas preferências de notificação, acesse as Configurações do Sistema.</p>
        </div>
    </div>
</body>
</html>