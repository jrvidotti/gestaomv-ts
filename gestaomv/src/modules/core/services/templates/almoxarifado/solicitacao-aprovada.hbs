<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>Solicitação Aprovada - Pronta para Atendimento</title>
    <style>
        body { font-family: Arial, sans-serif; line-height: 1.6; color: #333; }
        .container { max-width: 650px; margin: 0 auto; padding: 20px; }
        .header { background: linear-gradient(135deg, #10b981, #059669); padding: 30px; border-radius: 8px; margin-bottom: 20px; text-align: center; color: white; }
        .content { background: white; padding: 30px; border: 1px solid #dee2e6; border-radius: 8px; }
        .solicitacao-info { background: #f0fdf4; padding: 20px; border-radius: 6px; margin: 20px 0; border-left: 4px solid #10b981; }
        .material-item { background: #f9fafb; padding: 12px; border-radius: 4px; margin: 8px 0; border: 1px solid #e5e7eb; }
        .footer { margin-top: 20px; padding-top: 20px; border-top: 1px solid #dee2e6; font-size: 12px; color: #6c757d; text-align: center; }
        .btn { display: inline-block; background: #10b981; color: white; padding: 12px 24px; text-decoration: none; border-radius: 6px; font-weight: bold; margin: 10px 5px; }
        .btn:hover { background: #059669; }
        .btn-primary { background: #3b82f6; }
        .btn-primary:hover { background: #2563eb; }
        .badge { background: #10b981; color: white; padding: 4px 8px; border-radius: 12px; font-size: 12px; font-weight: bold; }
        .approved-by { background: #ecfdf5; padding: 15px; border-radius: 6px; margin: 15px 0; border: 1px solid #10b981; }
        .priority-notice { background: #fef3c7; padding: 15px; border-radius: 6px; border-left: 4px solid #f59e0b; margin: 20px 0; }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <div style="font-size: 48px; margin-bottom: 10px;">✅</div>
            <h1 style="margin: 0; font-size: 28px;">Solicitação Aprovada</h1>
            <p style="margin: 10px 0 0 0; font-size: 16px; opacity: 0.9;">Pronta para atendimento</p>
        </div>
        
        <div class="content">
            <h2>Olá, {{gerenteNome}}!</h2>
            
            <p>Uma solicitação de material foi <strong>aprovada</strong> e está pronta para ser atendida pela sua equipe.</p>
            
            <div class="solicitacao-info">
                <h3 style="margin-top: 0; color: #10b981;">📋 Informações da Solicitação</h3>
                <p><strong>Número:</strong> <span class="badge">#{{formatNumber solicitacao.id}}</span></p>
                <p><strong>Solicitante:</strong> {{solicitacao.solicitante.name}} ({{solicitacao.solicitante.email}})</p>
                <p><strong>Unidade de Destino:</strong> {{solicitacao.unidade.nome}}</p>
                <p><strong>Data da Solicitação:</strong> {{formatDate solicitacao.dataOperacao}}</p>
                <p><strong>Data de Aprovação:</strong> {{formatDate solicitacao.dataAprovacao}}</p>
                {{#if solicitacao.observacoes}}
                <p><strong>Observações:</strong> {{solicitacao.observacoes}}</p>
                {{/if}}
            </div>
            
            {{#if solicitacao.aprovador}}
            <div class="approved-by">
                <h4 style="margin-top: 0; color: #059669;">👤 Aprovado por</h4>
                <p style="margin-bottom: 0;"><strong>{{solicitacao.aprovador.name}}</strong> • {{solicitacao.aprovador.email}}</p>
            </div>
            {{/if}}
            
            <h3>📦 Materiais para Atendimento</h3>
            {{#each materiais}}
            <div class="material-item">
                <div style="display: flex; justify-content: space-between; align-items: center;">
                    <div>
                        <strong>{{material.nome}}</strong>
                        <br>
                        <small style="color: #6b7280;">{{material.tipoMaterial.nome}} • {{material.unidadeMedida.nome}}</small>
                    </div>
                    <div style="text-align: right;">
                        <strong style="color: #10b981;">{{qtdAtendida}} {{material.unidadeMedida.simbolo}}</strong>
                        {{#if (ne qtdAtendida qtdSolicitada)}}
                        <br>
                        <small style="color: #6b7280;">Solicitado: {{qtdSolicitada}}</small>
                        {{/if}}
                        <br>
                        <small style="color: #059669;">{{formatCurrency material.valorUnitario}} / unid.</small>
                    </div>
                </div>
            </div>
            {{/each}}
            
            <div class="priority-notice">
                <h4 style="margin-top: 0; color: #f59e0b;">⚡ Próximos Passos</h4>
                <ul style="margin-bottom: 0;">
                    <li>Separar os materiais conforme as quantidades aprovadas</li>
                    <li>Verificar disponibilidade no estoque</li>
                    <li>Confirmar as quantidades que serão efetivamente atendidas</li>
                    <li>Marcar a solicitação como <strong>ATENDIDA</strong> após separação</li>
                </ul>
            </div>
            
            <div style="text-align: center; margin: 30px 0;">
                <a href="{{systemUrl}}/admin/almoxarifado/solicitacoes/{{solicitacao.id}}" class="btn">
                    🏃‍♂️ Atender Solicitação
                </a>
                <a href="{{systemUrl}}/admin/almoxarifado/solicitacoes" class="btn btn-primary">
                    📋 Ver Todas as Solicitações
                </a>
            </div>
            
            <div style="margin-top: 20px; padding: 15px; background: #e0f2fe; border-radius: 6px; border-left: 4px solid #0284c7;">
                <p style="margin: 0;"><strong>💡 Dica:</strong> Você pode ajustar as quantidades durante o atendimento caso não haja estoque suficiente.</p>
            </div>
        </div>
        
        <div class="footer">
            <p>Esta é uma notificação automática do Sistema de Gestão de Almoxarifado.</p>
            <p>Para gerenciar suas preferências de notificação, acesse as Configurações do Sistema.</p>
        </div>
    </div>
</body>
</html>